# SwiftPM Workspace Guide

This folder is reserved for Xcode Swift Package Manager workspace metadata.

- Do not store secrets or machine-specific paths here.
- Keep package resolution/configuration files reviewable if dependencies are added later.
