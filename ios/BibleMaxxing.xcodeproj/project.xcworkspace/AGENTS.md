# Xcode Workspace Guide

This generated workspace package belongs to the BibleMaxxing Xcode project.

- Commit only small shared metadata needed to open or build the project.
- Do not commit user-specific Xcode state, DerivedData, or build outputs.
