# Xcode Schemes Guide

This folder contains shared Xcode schemes.

- Keep schemes deterministic and target only the BibleMaxxing app unless a new test target is added.
- Do not encode user-specific devices or signing identities.
