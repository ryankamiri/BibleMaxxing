# Shared Xcode Data Guide

This folder contains shared Xcode metadata that should be committed.

- Keep shared schemes here when they help CLI builds and handoff.
- Do not add user-specific workspace state, breakpoints, or local IDE preferences.
